openapi: 3.2.0
info:
  title: Opply Welcome Modal API
  version: 0.0.0
tags:
- name: Welcome Modal
paths:
  /api/v1/users/welcome-modal/me/:
    get:
      operationId: api_v1_users_welcome_modal_me_retrieve
      description: Per-user dismissal state for the post-relaunch welcome modal.
      tags:
      - Welcome Modal
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WelcomeModalState'
          description: ''
      summary: Api v1 users welcome modal me retrieve
      x-summary-source: derived
    post:
      operationId: api_v1_users_welcome_modal_me_create
      description: Per-user dismissal state for the post-relaunch welcome modal.
      tags:
      - Welcome Modal
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/WelcomeModalDismissalRequest'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/WelcomeModalDismissalRequest'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/WelcomeModalDismissalRequest'
        required: true
      security:
      - tokenAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WelcomeModalDismissal'
          description: ''
      summary: Api v1 users welcome modal me create
      x-summary-source: derived
components:
  schemas:
    NullEnum:
      enum:
      - null
    WelcomeModalDismissalRequest:
      type: object
      description: Inbound payload for `POST /users/welcome-modal/me/`.
      properties:
        action:
          $ref: '#/components/schemas/WelcomeModalActionEnum'
      required:
      - action
    WelcomeModalActionEnum:
      enum:
      - place_an_order
      - payments
      - explore
      type: string
      description: '* `place_an_order` - Place an order

        * `payments` - Payments

        * `explore` - Explore'
    WelcomeModalDismissal:
      type: object
      properties:
        action:
          allOf:
          - $ref: '#/components/schemas/WelcomeModalActionEnum'
          description: 'Which CTA the user chose when dismissing the welcome modal.


            * `place_an_order` - Place an order

            * `payments` - Payments

            * `explore` - Explore'
        dismissed_at:
          type: string
          format: date-time
          readOnly: true
      required:
      - action
      - dismissed_at
    WelcomeModalState:
      type: object
      description: Response shape for `GET /users/welcome-modal/me/`.
      properties:
        dismissed:
          type: boolean
        action:
          oneOf:
          - $ref: '#/components/schemas/WelcomeModalActionEnum'
          - $ref: '#/components/schemas/NullEnum'
        dismissed_at:
          type:
          - string
          - 'null'
          format: date-time
      required:
      - action
      - dismissed
      - dismissed_at
  securitySchemes:
    cookieAuth:
      type: apiKey
      in: cookie
      name: sessionid
    tokenAuth:
      type: apiKey
      in: header
      name: Authorization
      description: Token-based authentication with required prefix "Token"
